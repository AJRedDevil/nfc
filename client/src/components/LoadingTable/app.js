import React from 'react';

const LoadingTable = () => (
  <nav className="level">
    <div className="level-item has-text-centered">
      <h3>Loading...</h3>
    </div>
  </nav>
);

export default LoadingTable;
